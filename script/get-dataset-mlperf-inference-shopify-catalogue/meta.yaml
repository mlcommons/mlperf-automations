alias: get-dataset-mlperf-inference-shopify-catalogue
uid: ca5cbb485e044ac4
automation_alias: script
automation_uid: 5b4e0237da074764

# Metadata
tags:
- get-dataset-mlperf-inference-shopify-catalogue

# Cache
cache: true

# Environment
new_env_keys:
- MLC_ML_DATASET_MLPERF_INFERENCE_SHOPIFY_CATALOGUE_DATASET_PATH
- MLC_ML_DATASET_FILE_WITH_PATH

# Variations
variations:
  mlc:
    default: true
    env:
      MLC_DOWNLOAD_SRC: mlcommons
    group: download-src
    prehook_deps:
    - enable_if_env:
        MLC_TMP_REQUIRE_DOWNLOAD:
        - 'yes'
      env:
        MLC_DOWNLOAD_FILENAME: global-shopify-catalogue-dataset
        MLC_DOWNLOAD_FINAL_ENV_NAME: MLC_ML_DATASET_MLPERF_INFERENCE_SHOPIFY_CATALOGUE_DATASET_PATH
        MLC_EXTRACT_FINAL_ENV_NAME: MLC_ML_DATASET_MLPERF_INFERENCE_SHOPIFY_CATALOGUE_DATASET_PATH
      extra_cache_tags: ml,dataset,shopfy,catalogue
      force_cache: true
      force_env_keys:
      - MLC_OUTDIRNAME
      names:
      - global-shopify-catalogue-dataset
      tags: download-and-extract
      update_tags_from_env_with_prefix:
        _url.:
        - MLC_DOWNLOAD_URL
  r2-downloader:
    adr:
      global-shopify-catalogue-dataset:
        env:
          MLC_DOWNLOAD_URL: https://inference.mlcommons-storage.org/metadata/VLM-data.uri
        tags: _r2-downloader
    default: true
    group: download-tool
  dry-run:
    env:
      MLC_DOWNLOAD_MODE: dry
    group: run-mode
  dry-run,r2-downloader:
    env:
      MLC_DOWNLOAD_EXTRA_OPTIONS: -x

# Output / debugging
print_env_at_the_end:
  MLC_ML_DATASET_MLPERF_INFERENCE_SHOPIFY_CATALOGUE_DATASET_PATH: Shopify catalogue dataset path

# Tests
tests:
  run_inputs:
  - variations_list:
    - r2-downloader,mlc,dry-run
